# shared-types

Cross-module type definitions — branded IDs (`TenantId`, `UserId`, …),
event payload contracts và shared DTOs. Type-only; không runtime code.


## Public surface (planned)

| Entry | Purpose |
|---|---|
| `shared-types` | Branded IDs, event payloads, shared DTOs |
